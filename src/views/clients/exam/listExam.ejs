<%- include('./../comon/header') %>
<link type="text/css" rel="stylesheet" href="/styles/clients/exam.css" />

<div class="container my-5">
  <div class="card shadow p-4 mb-4">
    <h5 class="card-title">Tìm kiếm đề thi</h5>
    <div class="alert alert-info mt-4">
      <i class="bi bi-info-circle me-2"></i>
      <strong>Lưu ý:</strong> Nhấp đúp chuột vào đề thi để bắt đầu làm bài. Chỉ
      những đề thi có trạng thái "Hoạt động" mới có thể làm được.
    </div>
    <form class="row g-3 align-items-center">
      <div class="col-md-9">
        <input
          type="text"
          class="form-control"
          id="searchExamCode"
          placeholder="Nhập mã đề thi cần tìm"
        />
      </div>
      <div class="col-md-3 d-grid">
        <button type="button" class="btn btn-primary" id="searchButton">
          <i class="fas fa-search"></i> Tìm kiếm
        </button>
      </div>
    </form>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-hover">
      <thead class="table-dark">
        <tr>
          <th class="text-center" style="width: 15%">Mã đề</th>
          <th class="text-center" style="width: 35%">Tên bài</th>
          <th class="text-center" style="width: 25%">Ngày tạo</th>
          <th class="text-center" style="width: 25%">Trạng thái</th>
        </tr>
      </thead>
      <tbody id="examTableBody"></tbody>
    </table>
  </div>

  <div id="loading" class="text-center d-none my-3">
    <div class="spinner-border text-primary" role="status">
      <span class="visually-hidden">Đang tải...</span>
    </div>
  </div>
</div>

<style>
  .toast {
    position: fixed;
    top: 20px;
    right: 20px;
    z-index: 1050;
  }

  .exam-row:hover {
    background-color: rgba(0, 0, 0, 0.05);
    transition: background-color 0.2s ease;
  }

  .exam-row[title] {
    cursor: not-allowed;
  }
</style>

<script type="module" src="/JS/clients/exam/services/listExam.js"></script>

<%- include('./../comon/footer') %>
